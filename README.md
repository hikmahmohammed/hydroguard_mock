# hydroguard_mock

A Node.js and Express.js mini-project demonstrating backend API and frontend integration for simulated HydroGuard device data.”


Run locally with:
npm install
node index.js

Then open `index.html` with Live Server to view results.
